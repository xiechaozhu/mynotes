比如我提交了一个目录a，以及他下面的一些目录和文件。

如果此时我更新了本地的一个文件，或者在一个目录下新加了一个文件，

此时我要更新远程仓库，那么我可以在a目录下 使用 git add *，git commit -m ”干了什么“，然后git push

此时远程仓库，只会在最新修改的地方显示”干了什么“。

在a目录下的某一个目录中添加或者修改某个文件，并在这个目录中进行git add *，git commit -m ”干了什么“，然后git push

这个时候默认更新整个a目录，并在最新修改的地方显示”干了什么“。